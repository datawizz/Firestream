[pytest]
testpaths =
    tests
python_files =
    test_*.py
python_functions = test_*
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
markers =
    integration: requires live external services (K3D, S3, Spark, Kafka)
addopts = -m "not integration"
