name: firestream-airflow-gke
description: >-
  GKE Autopilot cluster, Artifact Registry, and GCP Secret Manager secrets for
  the Firestream Airflow deployment example.
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
config:
  pulumi:tags:
    value:
      pulumi:template: python
